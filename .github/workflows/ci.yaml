name: Running Pytest testcasesrtrt

on:
  push:
    branches: qa-teja

  pull_request:
    branches:
      - master
jobs:
  testingpytest:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirement.txt

    - name: Run tests
      run: |
        cd tests
        pytest -m repo --token=${{ secrets.TOKEN }}